# Swift / SwiftPM
.build/
.swiftpm/
Package.resolved

# Build output
DerivedData/
*.app
*.dSYM/
dist/

# macOS
.DS_Store

# Signing secrets — never commit
*.p12
*.cer
*.provisionprofile
notarize.env
secrets.env
# Local, machine-specific signing identity name read by build.sh (personal cert — keep out of the repo)
.signing-identity

# build.sh output
build/
